{
  "name": "sparify",
  "version": "1.0.0",
  "description": "client side routing is provided by [page.js](https://visionmedia.github.io/page.js/) . It allows declaring a path using express style. for example ``` page('/home', showHomePage); page('/about', showAboutPage); page('/contact', showContactPage); ```",
  "main": "app/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "budo app/app.js:dist/bundle.js --live --open --pushstate",
    "build": "browserify app/app.js -o dist/bundle.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "budo": "^9.4.7",
    "scssify": "^2.2.1"
  },
  "dependencies": {
    "bootstrap-sass": "^3.3.7",
    "diffhtml": "^0.9.2",
    "page": "^1.7.1",
    "yo-yo": "^1.4.0"
  },
  "browserify": {
    "transform": [
      [
        "scssify",
        {}
      ]
    ]
  }
}
